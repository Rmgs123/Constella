# 🛰️ Constella

**Constella** — одноранговая сеть VPS-узлов с автоматическим выбором лидера и Telegram-ботом.
Каждый сервер является равноправным участником сети; лидер выбирается автоматически из «живых» узлов.
Бот позволяет просматривать состояние узлов и управлять ими без выделенного центрального сервера.

---

## 📈 Прогресс

Создается вместе с **@mvln** и **ElBruno**



---

## 🚀 Введение

**Цель проекта:**
Создать простую и устойчивую одноранговую сеть серверов с автоматическим выбором лидера и Telegram-интерфейсом.
Новые узлы подключаются по токену без ручного редактирования IP-адресов; лидер выбирается автоматически.

**Ключевые возможности MVP:**

* Автоматический выбор и смена лидера («Хост»).
* Подключение новых узлов по одноразовому токену.
* Telegram-бот для просмотра метрик и перезагрузки узлов.
* Полностью децентрализованная работа (без выделенного центра).

---

## 🧭 Область проекта

**Входит в MVP:**

* P2P-членство и heartbeat.
* Авто-лидер из живых узлов.
* Telegram-бот на лидере.
* Join по токену с TTL.
* Просмотр списка узлов и пометка лидера.
* Запрос метрик (CPU, RAM, диск) и перезагрузка узла.

**Не входит (на этапе MVP):**

* Потоковый мониторинг и алерты.
* Docker-операции.
* NAT-traversal и смена владельца из бота.
* RBAC, аудит, веб-панель.

---

## 🧩 Основные сценарии (Use Cases)

1. **Инициализация сети** — запуск первого узла и задание Telegram-ника владельца (`@username`).
2. **Выдача приглашения** — команда `/invite` генерирует join-ссылку с TTL.
3. **Подключение нового узла** — `install.sh join` с join-ссылкой.
4. **Просмотр списка узлов** — бот показывает имена и выделяет текущего лидера (**Хост**).
5. **Запрос метрик** — бот запрашивает CPU/RAM/диск выбранного узла.
6. **Перезагрузка узла** — команда из бота выполняет `reboot`.
7. **Смена лидера** — при отказе текущего лидера выбирается новый из «живых».

---

## ⚙️ Требования

### Функциональные

* P2P-членство и heartbeat.
* Автовыбор лидера по минимальному `LeaderKey=(priority, NodeID)`.
* Telegram-бот запускается только на лидере.
* Join-процесс по одноразовому токену.
* RPC-вызовы: `GetPeers`, `GetStats`, `Reboot`, `Join`.
* Автоматическая рассылка peer-списка при join/leave.
* **Ограничение доступа:** бот отвечает только пользователю с заданным `@owner_username`.

### Нефункциональные

* Фейловер лидера ≤ 10 с.
* Простая установка (`git clone` + `install.sh`).
* Шифрование (TLS или HMAC).
* Нагрузка: CPU < 2 %, RAM < 100 МБ.
* Совместимость: Linux x86_64/ARM64.

---

## 🏗️ Архитектура

**Компоненты узла:**

* **Membership & Heartbeat** — обмен статусами, список «живых».
* **RPC-API** — обработка команд и запросов.
* **Leader Services** — Telegram-бот и логика координации (активны только у лидера).

**Взаимодействие:**
бот → лидер → RPC → нужный узел.
Heartbeat между всеми узлами «каждый-с-каждым».

---

## 🌐 Сеть и протоколы

* **Порт:** 4747/TCP.
* **Шифрование:** TLS (самоподписанный CA) или HMAC с `NetworkSecret`.
* **Heartbeat:** ping каждые 2 с, узел считается «down» после 3 пропусков.
* **Discovery:** seed-узлы + автообновление peer-списка при join/leave.
* **Лидерство:** определяется детерминированно по минимальному `LeaderKey` среди живых.

---

## 🔑 Подключение (Join)

**Формат ссылки:**

```
join://<seed-host>:4747?net=<NetworkID>&token=<OneTimeToken>&ttl=<15m>
```

**Этапы:**

1. Проверка токена.
2. Получение `NetworkID`, `NetworkSecret`, `peer-list`, `owner_username`.
3. Регистрация имени узла.
4. Старт heartbeat и обновление peer-списков.

---

## 💬 Telegram-интерфейс

* Главное меню: список узлов

  ```
  • delta-01 — Хост  
  • echo-02  
  • fox-03 (offline)
  ```
* Карточка узла:

  * **Показать потребление** — CPU, RAM, диск.
  * **Перезагрузить сервер** — подтверждение → `reboot`.
* `/invite <ttl>` — выдаёт join-ссылку.
* Бот отвечает **только** пользователю с ником `@owner_username`.

---

## 🧠 RPC API

| Метод              | Назначение                                 |
| ------------------ | ------------------------------------------ |
| `GetPeers()`       | список узлов {name, node_id, addr, status} |
| `GetStats(target)` | метрики CPU/RAM/диск и аптайм              |
| `Reboot(target)`   | перезагрузка узла                          |
| `Join(req)`        | первичное подключение по токену            |

**Формат:** JSON over TLS(TCP) (MVP) → в будущем gRPC.

---

## 📂 Общее хранилище сети

Каждый узел хранит файл `state/network_state.json` (права 600).

**Содержимое:**

```json
{
  "network_id": "abcd1234",
  "owner_username": "@Rmgss",
  "network_secret": "****",
  "peers": [
    {"name": "delta-01", "addr": "203.0.113.10:4747", "node_id": "...", "status": "alive"},
    {"name": "echo-02",  "addr": "203.0.113.11:4747", "node_id": "...", "status": "alive"}
  ]
}
```

**Обновление:**
при `init/join` seed выдаёт актуальное состояние;
далее периодическая сверка (peer-reconcile) каждые 30 с.

**Смена владельца:** не входит в MVP (позже через ручное редактирование и рассылку обновления).

---

## 🛠️ Развёртывание

**Первый узел:**

```bash
install.sh init --server-name delta-01 \
  --public-addr 203.0.113.10:4747 \
  --owner @Rmgss
```

**Новый узел:**

```bash
install.sh join --server-name echo-02 \
  --join "join://203.0.113.10:4747?net=abcd1234&token=XYZ&ttl=15m"
```

---

## ⚡ Эксплуатация

CLI команды: `status`, `peers`, `invite`, `leave`
Логи узлов → ротация.
Обновление кластеров → rolling restart без простоя.

---

## 🧪 Тестирование и приёмка

* Init с заданием `@owner_username`.
* Join 2–3 узлов без ручной правки IP.
* Список узлов, верный «Хост».
* Метрики и перезагрузка работают.
* Фейловер лидера ≤ 10 с.
* Проверка отказа по неверному токену / TTL.

---

## ⚠️ Ограничения и риски

* Открыт порт 4747 у хотя бы одного seed-узла.
* NAT не поддерживается (MVP).
* Перезагрузка требует CAP_SYS_BOOT или root.

---

## 🔮 План развития

* Потоковые метрики и уведомления в бот.
* Управление Docker-контейнерами.
* RBAC и аудит-лог.
* Смена владельца через CLI.
* NAT-traversal (WireGuard/libp2p).
